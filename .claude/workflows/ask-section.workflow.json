{
  "name": "askSection",
  "description": "Ask questions on selected textbook content",
  "steps": [
    {
      "id": "extract",
      "action": "rag.extractSection"
    },
    {
      "id": "answer",
      "action": "llm.answer",
      "input": {
        "context": "{{steps.extract.output.section}}",
        "question": "{{question}}"
      }
    }
  ]
}
